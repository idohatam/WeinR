% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Reporter.R
\name{CreateReport}
\alias{CreateReport}
\title{Create an HTML QC report from a LongReadQC object}
\usage{
CreateReport(
  path = "report.Rmd",
  title = "My Report",
  author = Sys.info()[["user"]],
  date = format(Sys.Date(), "\%B \%d, \%Y"),
  toc = TRUE,
  code_folding = c("none", "show", "hide"),
  theme = "cosmo",
  highlight = "tango",
  overwrite = FALSE,
  render_html = TRUE,
  open_browser = interactive(),
  mfa = NULL
)
}
\arguments{
\item{path}{Character. Output path for the report \strong{without or with} the \code{.Rmd}
extension. If no extension is provided, \code{.Rmd} is appended. Directories are
created if needed.}

\item{title}{Character. Report title shown in the rendered HTML.}

\item{author}{Character. Report author shown in the rendered HTML.}

\item{date}{Character. Report date shown in the rendered HTML.}

\item{toc}{Logical. Whether to include a table of contents.}

\item{code_folding}{Character. One of \code{"none"}, \code{"show"}, \code{"hide"}. Controls
code folding in the HTML output.}

\item{theme}{Character. R Markdown Bootstrap theme name (e.g. \code{"cosmo"}).}

\item{highlight}{Character. Syntax highlighting theme (e.g. \code{"tango"}).}

\item{overwrite}{Logical. If \code{FALSE} and the output \code{.Rmd} exists, an error is
thrown. If \code{TRUE}, existing files are replaced.}

\item{render_html}{Logical. If \code{TRUE}, renders the \code{.Rmd} to HTML via
\code{rmarkdown::render()}.}

\item{open_browser}{Logical. If \code{TRUE} and \code{render_html=TRUE}, opens the
rendered HTML in a browser.}

\item{mfa}{A \code{LongReadQC} object (required). Used by the report as
\code{params$mfa}.}
}
\value{
If \code{render_html=TRUE}, an (invisible) list with elements \code{rmd} and
\code{html} giving normalized paths to the generated files. If \code{render_html=FALSE},
an (invisible) list with element \code{rmd}.
}
\description{
Writes an R Markdown (\code{.Rmd}) file to \code{path} and (optionally) renders it to
an HTML report using \code{rmarkdown::render()}. The report includes a DT summary
table and plot sections assembled from \code{mfa} (a \code{LongReadQC} object).
}
\details{
The stylesheet is shipped with the package at \code{inst/styles/colorblind.css}.
For portability, it is copied next to the generated \code{.Rmd} and referenced
using a relative path in the YAML header.
}
