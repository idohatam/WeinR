% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/QualFilter.R
\name{QualFilter}
\alias{QualFilter}
\title{Filter reads across all files in a LongReadQC object}
\usage{
QualFilter(
  qc_obj,
  OutDir = ".",
  MinAvgQS = 20,
  MinLength = 100,
  MaxNumberNs = 2,
  OutFileType = c("fastq")
)
}
\arguments{
\item{qc_obj}{A \code{LongReadQC} object with per-read metrics already computed
(e.g., via \code{QualMat()}).}

\item{OutDir}{Character(1). Output directory where filtered read files will be
written. Created if it does not exist.}

\item{MinAvgQS}{Numeric(1). Minimum mean per-read Q-score required to keep a read.}

\item{MinLength}{Integer(1). Minimum read length required to keep a read.}

\item{MaxNumberNs}{Integer(1). Maximum number of \code{N} bases allowed per read.}

\item{OutFileType}{Character vector specifying output format(s). Supported values
are \code{"fastq"}, \code{"fasta"}, and \code{"bam"}. Multiple formats may be
requested.}
}
\value{
A \code{LongReadQC} object with per-file filtering results stored in
\code{qc_obj@metadata[[file]]$filter_summary}.
}
\description{
Applies per-read filters (mean Q-score, read length, and N-count) to each file
in a \code{LongReadQC} object and writes filtered reads to disk. A filtering
summary and output paths are stored in \code{qc_obj@metadata} for each file.
}
\examples{
\dontrun{
qc_obj <- QualMat(qc_obj)  # ensure metrics exist first
qc_filt <- QualFilter(
  qc_obj,
  OutDir = "filtered_reads",
  MinAvgQS = 20,
  MinLength = 100,
  MaxNumberNs = 2,
  OutFileType = c("fastq", "fasta")
)
}

}
\seealso{
\code{\link{FilterLong}} for single-file filtering and
\code{\link{QualMat}} for computing per-read metrics used by filtering.
}
