% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/QualViz.R
\name{QualViz}
\alias{QualViz}
\title{Run QC on input files and return a LongReadQC object}
\usage{
QualViz(files, outpath, title)
}
\arguments{
\item{files}{Character vector of input file paths (e.g., FASTQ files).}

\item{outpath}{Character(1). Output base path (used to create the output
directory if needed). Currently not used to write files in this function.}

\item{title}{Character(1). Title string (currently unused in this function).}
}
\value{
A \code{LongReadQC} object containing computed metrics and plots.
}
\description{
Internal convenience wrapper that initializes a \code{LongReadQC} object,
imports each input file, computes metrics, and generates plots.
This function returns the populated \code{LongReadQC} object and does not
render a report (report rendering is handled elsewhere).
}
\details{
This function is intended for internal use and is not part of the public API.
It assumes \code{files} are readable by \code{ImportFile()} and that the internal helpers
\code{.init_qc_object()}, \code{QualMat()}, and \code{QualPlot()} exist.
}
\examples{
\dontrun{
qc <- WeinR:::QualViz(files = fastq_files, outpath = "reports/qc", title = "QC")
}

}
\keyword{internal}
