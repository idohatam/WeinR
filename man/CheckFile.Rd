% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CheckFile.R
\name{CheckFile}
\alias{CheckFile}
\title{Validate an input file path and infer file type}
\usage{
CheckFile(filePath)
}
\arguments{
\item{filePath}{Character scalar. Path to an input file.}
}
\value{
A length-1 character string: one of \code{"bam"},
\code{"fastq"}, or \code{"fastq.gz"}.
}
\description{
Internal helper that validates a single input file path, issues a warning
for large files, and infers the input type from the filename
extension.
}
\details{
Supported extensions are BAM (\code{.bam}), FASTQ (\code{.fastq}, \code{.fq}),
and gzipped FASTQ (\code{.fastq.gz}, \code{.fq.gz}).

This function is intended for internal use only and is not part of the
public API. It errors on invalid paths or unsupported file extensions.
}
\examples{
\dontrun{
# FASTQ example
fq <- tempfile(fileext = ".fastq")
file.create(fq)
WeinR:::CheckFile(fq)

# gzipped FASTQ example
fqgz <- tempfile(fileext = ".fastq.gz")
file.create(fqgz)
WeinR:::CheckFile(fqgz)

# BAM example
bam <- tempfile(fileext = ".bam")
file.create(bam)
WeinR:::CheckFile(bam)
}

}
\keyword{internal}
